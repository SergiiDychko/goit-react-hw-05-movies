import styled from 'styled-components';

export const StyledCast = styled.ul`
  display: flex;
  gap: 20px;
  flex-wrap: wrap;
  justify-content: space-around;
  margin-bottom: 20px;
  .notify {
    font-size: 18px;
    font-weight: 500;
  }
`;
